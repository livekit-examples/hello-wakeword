[project]
name = "hello-wakeword"
version = "0.1.0"
description = "Wakeword detection with voice agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "livekit-wakeword>=0.1.3",
]

[tool.uv.workspace]
members = ["client", "agent"]
